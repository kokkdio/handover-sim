# local

This folder is for user-specific additions and experiments that should stay separate from the original framework.

- `local/data/`: user data files, temporary datasets, and artifacts.
- `local/scripts/`: helper scripts or custom runners.
- `local/src/`: optional standalone Python modules or local extensions.
- `local/tests/`: tests for local code.

Place future files here instead of editing the original framework structure.
